[package]
name = "board_interface"
version = "0.1.0"
edition = "2021"

[dependencies]
# Include the name of your library crate as a dependency
board = { path = "../board" }

[[bin]]
name = "board_interface"
path = "main.rs"